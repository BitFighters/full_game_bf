
export enum FRIENDS_STATUS {
  
  // when this user has sent the friend request.
  FRIEND_REQUEST_SENT = 10,

  // when this user has pending friend request of another guy.
  FRIEND_REQUEST_PENDING = 20,

  // when this user accepts the friend request.. 
  FRIENDS = 30,
}